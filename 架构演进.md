## 1. 产品定位：仍是“一次性规划工具”，缺乏“持续运营”闭环

**现状**：
当前产品聚焦于从商业画布到变革路标的**一次性规划交付**，输出一份静态蓝图。用户完成规划后，工具的价值就结束了——没有后续的**实施跟踪、效果度量、动态调整**。

**问题**：
- 企业数字化转型是持续性工作，规划只是起点。无法跟踪能力成熟度是否按路标提升、变革举措是否落地、业务指标是否改善，产品就沦为“战略PPT生成器”。
- 这与“深度绑定客户对AI依赖”的目标矛盾：客户完成一次规划后可能半年才用一次，根本无法形成持续订阅。
- 华为4A架构本身强调“持续迭代的架构治理”，而非一次性蓝图。

**建议**：
重构产品定位为 **“规划→执行→评估→优化”的持续运营平台**：
- 增加 **“实施追踪”模块**：将能力块差距与变革举措关联实际项目进展（如Jira集成或手动里程碑检查）。
- 增加 **“成熟度再评估”**：支持按季度对能力块二次打分，自动生成热力图对比，观察改善效果。
- 增加 **“运营仪表盘”**：展示规划与实际执行的偏差、关键指标的动态趋势。
- **商业模式**：转为“订阅制+按评估次数付费”，而非“按项目许可费”。

---

## 2. 垂域专业性：价值流与能力框架的关联缺乏“场景因子”深度

**现状**：
demo中价值流阶段与能力块的映射是直接列表关联（如“订单履行”关联“库存管理、信用审核”），没有体现**场景因子的差异化影响**（不同场景下同一能力块的表现可能天差地别）。

**问题**：
- 华为4A架构的价值流分析核心是“场景因子驱动能力要求”——同一能力块在不同场景（如“普通订单”vs“加急订单”）下，对效率、质量、风险的要求不同，差距评估也应不同。但当前没有区分场景，差距评估是全局统一的，这严重降低了诊断的精准性。
- 客户会质疑：“你们说我的‘信用审核’能力是红色，但我的‘加急订单’场景下信用审核跑得很快，为什么标红？”——这种质疑会伤害专业信任。

**建议**：
引入 **“场景-能力-维度”三维矩阵**：
- 每个价值流阶段下，列出多个场景因子（如高频/低频、高价值/低价值）。
- 每个能力块在**每个场景**下都有独立的五维评分（效率、质量、成本、风险、体验）。
- 最终能力块的“综合评分”根据场景频次/权重加权得出。
- 热力图支持按场景切换视图：选择“加急订单”场景时，看到的是该场景下的能力热力分布。
- 技术实现：将当前“capabilityScores”从每个能力块一个评分，扩展为每个能力块每个场景一个评分对象。

---

## 3. 架构演进：缺乏“企业级多项目组合管理”与“知识沉淀复用”能力

**现状**：
当前产品面向**单个项目**（一个企业/一个事业部的一次规划）。用户无法同时管理多个项目的规划、对比不同业务单元的能力差距、复用之前的规划成果。

**问题**：
- 如果面向咨询公司或大型集团，他们需要管理多个客户的规划资产——当前单项目存档（导出HTML/Excel）会导致知识孤岛。
- AI的咨询价值高度依赖历史案例积累：当前没有设计**跨项目知识库**，AI无法从过去10个项目中学习规律（如“电子行业普遍在‘供应链协同’能力上落后制造业”）。没有数据积累，AI建议将永远停留在通用模板层面，无法体现“近20年顶尖科技企业经验”的差异化。
- 华为4A架构的“能力框架”应该是一个可跨项目复用的**企业标准库**，而非每个项目独立维护一份清单。

**建议**：
引入 **“资产库”的架构升级计划**：

- **数据层**：增加`project`和`organization`实体z状态、转型进度对比。

---

**总结**：这三个建议分别对应了：
1. **产品生命周期**：从单次交付到持续运营（解决商业模式问题）
2. **专业深度**：从粗粒度到场景化精细诊断（解决可信度问题）
3. **系统扩展**：从孤岛项目到企业级知识资产（解决平台化问题）

---

## 附录：与前端快照的对应关系（实现备忘）

| 文档小节 | 快照 / 状态字段 | 说明 |
|---------|----------------|------|
| §3 组织与项目 | `workspaceMeta` | 仅存快照字段（默认空）；随 `WORKFLOW_SNAPSHOT_KEYS` 写入资产库；可由后续接入/API 写入 |
| §1 持续运营 | `continuousOps` | 检查点、成熟度再评估等占位结构，供后续「实施追踪 / 仪表盘」挂载 |
| §3 知识沉淀 | `knowledgeRefs` | 匿名案例 ID、关联资产记录 ID 占位 |
| §2 场景因子 | `heatmapScenarioView` + 能力行 `scenarioGapProfiles` | 步骤 5 下拉切换热力视图；Excel「场景四维JSON」列可导入分场景四维差距 |